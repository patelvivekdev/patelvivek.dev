---
title: How to add toast
description: How to add toast in React/Next JS
slug: toast
published: true
publishedAt: April 29, 2024
tags: [Next, React, JSX, Markdown, Blog, Toast, React-Hot-Toast]
---

## Update: Dec 09, 2024

<Callout type="important">
  The previous version of this article used [react-hot-toast](https://react-hot-toast.com/).

Currently, I am using [Sonner](https://sonner.emilkowal.ski/).

</Callout>

## Install

```bash
# react-hot-toast
# bun add react-hot-toast

bun add sonner
```

## Add toast

### Adding Toaster To root Layout

```tsx
// import { Toaster } from 'react-hot-toast';
import { Toaster } from 'sonner';

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang='en'>
      <body className={inter.className}>
        <div className='container'></div>
        {children}
        <Toaster position='top-right' />
      </body>
    </html>
  );
}
```

### Using toast in Components

```tsx
// import toast from 'react-hot-toast';
import { toast } from 'sonner';

const handleClick = () => {
  toast.success('This is a success toast');
};

export default function App() {
  return (
    <div>
      <button onClick={handleClick}>Show Toast</button>
    </div>
  );
}
```

## Add toast with options

### Types

- Success
- Error
- Loading

### Example

```tsx
toast.success('This is a success toast');
toast.error('This is an error toast');
toast.loading('This is a loading toast');
```

## Link

- Learn More About [React-Hot-Toast API ](https://react-hot-toast.com/docs)
- Learn More About [Sonner API ](https://sonner.emilkowal.ski/getting-started)
- Github repo of [react-hot-toast](https://github.com/timolins/react-hot-toast)
- Github repo of [Sonner](https://github.com/emilkowalski/sonner)
